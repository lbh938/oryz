import { ReactNode } from 'react';

export const metadata = {
  title: 'Mode TV - ORYZ STREAM',
  description: 'Interface optimisée pour télévision',
};

export default function TVLayout({ children }: { children: ReactNode }) {
  return children;
}

